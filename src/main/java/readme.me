This is the backend for the Collective project.

It uses Jersey 2.0 REST architecture to deploy its service endpoints and is configured to run off a Tomcat server.
It is configured to use Maven to manage dependencies.

The test client in the default package is used to do dry runs on the architecture.