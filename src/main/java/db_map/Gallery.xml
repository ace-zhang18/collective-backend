<?xml version = "1.0" encoding = "UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "Gallery">

   <insert id = "insert" parameterType = "Gallery">
      	INSERT INTO galleries (id, cover, title, collection, tags, permissions, history, owners, sale) 
      	VALUES (#{id}, #{cover}, #{title}, #{collection}, #{tags}, #{permissions}, #{history}, #{owners}, #{sale});
	</insert> 
	
	<update id = "update" parameterType = "Gallery">
    	UPDATE galleries SET cover = #{cover}, title = #{title}, collection = #{collection}, tags = #{tags}, 
    	permissions = #{permissions}, history = #{history}, owners = #{owners}, sale = #{sale}
    	WHERE id = #{id};
	</update>
    
	<delete id = "delete" parameterType = "long">
    	DELETE FROM galleries WHERE id = #{id};
    </delete>
    
    <select id="get" parameterType = "long" resultMap="galleryMap">
    	SELECT * FROM galleries WHERE id = #{id};
	</select>

	<select id="getSet" parameterType = "long" resultMap="galleryMap">
    	SELECT * FROM galleries WHERE id IN 
    	<foreach item="item" index="index" collection="array"
             open="(" separator="," close=")">
        	#{item}
    	</foreach>
	</select>
    
	<select id="getAll" resultMap="galleryMap">
    	SELECT * FROM galleries ORDER BY id ASC;
	</select>
    
	<resultMap id = "galleryMap" type = "Gallery">
		<result property = "id" column = "ID"/>
		<result property = "cover" column = "COVER"/>
		<result property = "title" column = "TITLE"/>
		<result property = "collection" column = "COLLECTION" typeHandler="handlers.LongArrayTypeHandler"/>
		<result property = "tags" column = "TAGS" typeHandler="handlers.LongArrayTypeHandler"/>
		<result property = "permissions" column = "PERMISSIONS" javaType="org.json.JSONObject" typeHandler="handlers.JSONObjectTypeHandler"/>
		<result property = "history" column = "HISTORY" javaType="org.json.JSONObject" typeHandler="handlers.JSONObjectTypeHandler"/>
		<result property = "owners" column = "OWNERS" javaType="org.json.JSONObject" typeHandler="handlers.JSONObjectTypeHandler"/>	
		<result property = "sale" column = "SALE" javaType="org.json.JSONArray" typeHandler="handlers.JSONArrayTypeHandler"/>
	</resultMap>

	
</mapper>