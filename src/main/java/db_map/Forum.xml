<?xml version = "1.0" encoding = "UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "Forum">

   <insert id = "insert" parameterType = "Forum">
      	INSERT INTO forums (id, name, parent, permissions) 
      	VALUES (#{id}, #{name} #{parent}, #{permissions});
	</insert> 
	
	<update id = "update" parameterType = "Forum">
    	UPDATE forums SET name = #{name}, parent = #{parent}, permissions = #{permissions}
    	WHERE id = #{id};
	</update>

    <delete id = "delete" parameterType = "long">
    	DELETE FROM forums WHERE id = #{id}
    </delete>
    
    <select id="get" parameterType = "long" resultMap="forumMap">
    	SELECT * FROM forums where id = #{id};
	</select>

	<select id="getSet" parameterType = "long" resultMap="forumMap">
    	SELECT * FROM forums WHERE id IN 
    	<foreach item="item" index="index" collection="array"
             open="(" separator="," close=")">
        	#{item}
    	</foreach>
	</select>

	<select id="getAll" resultMap="forumMap">
    	SELECT * FROM forums ORDER BY id ASC;
	</select>
	
	<select id="getSub" resultMap="forumMap">
		SELECT * FROM forums WHERE parent = #{id};
	</select>
    
	<resultMap id = "forumMap" type = "Forum">
		<result property = "id" column = "ID"/>
		<result property = "name" column = "NAME"/>
		<result property = "parent" column = "PARENT"/>
		<result property = "permissions" column = "PERMISSIONS" javaType="org.json.JSONObject" typeHandler="handlers.JSONObjectTypeHandler"/>
	</resultMap>

	
</mapper>