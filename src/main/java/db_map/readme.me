This backend is configured to interface with a PostgreSQL database

The db_map package contains the specifications required for the PostgreSQL connector to format the data between the DAOs and the Database's tables.

SqlMapConfig.xml contains the primary settings needed to make the connection to the database.