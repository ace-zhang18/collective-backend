<?xml version = "1.0" encoding = "UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "StaffRole">

   <insert id = "insert" parameterType = "StaffRole">
      	INSERT INTO staff_roles (ID, NAME, PERMISSIONS) VALUES (#{id}, #{name}, #{permissions});
	</insert> 
	
	<update id = "update" parameterType = "StaffRole">
    	UPDATE staff_roles SET NAME = #{name}, PERMISSIONS = #{permissions} WHERE id = #{id};
	</update>
    
	<delete id = "delete" parameterType = "long">
    	DELETE FROM staff_roles WHERE id = #{id}
    </delete>
    
    <select id="get" parameterType = "long" resultMap="staffRoleMap">
    	SELECT * FROM staff_roles WHERE id = #{id};
	</select>

	<select id="getSet" parameterType = "long" resultMap="staffRoleMap">
    	SELECT * FROM staff_roles WHERE id IN 
    	<foreach item="item" index="index" collection="array"
             open="(" separator="," close=")">
        	#{item}
    	</foreach>
	</select>

	<select id="getAll" resultMap="staffRoleMap">
    	SELECT * FROM staff_roles ORDER BY id ASC;
	</select>
    	
	<resultMap id = "staffRoleMap" type = "StaffRole">
	   	<result property = "id" column = "ID"/>
   		<result property = "name" column = "NAME"/>
   		<result property = "permissions" column = "PERMISSIONS" javaType="org.json.JSONObject" typeHandler="handlers.JSONObjectTypeHandler"/>
	</resultMap>
	
</mapper>